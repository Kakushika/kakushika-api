machine:
  node:
    version: 0.12.7

dependencies:
  override:
    - git clone ${DEPLOY_TARGET_REPOSITORY} deployment
    - npm install -g gulp mocha
    - npm install --dev

deployment:
  production:
    branch: master
    commands:
      - gulp
      - chmod +x ./deploy.sh
      - ./deploy.sh
