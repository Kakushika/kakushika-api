machine:
  node:
    version: 5.0.0

dependencies:
  override:
    - git clone ${DEPLOY_TARGET_REPOSITORY} deployment
    - npm install -g gulp mocha
    - npm install

deployment:
  production:
    branch: master
    commands:
      - gulp
      - chmod +x ./deploy.sh
      - ./deploy.sh
