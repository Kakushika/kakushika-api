machine:
  node:
    version: 4.1.2

dependencies:
  override:
    - git clone ${DEPLOY_TARGET_REPOSITORY} deployment
    - npm install -g gulp mocha
    - npm install

deployment:
  production:
    branch: master
    commands:
      - gulp
      - chmod +x ./deploy.sh
      - ./deploy.sh
